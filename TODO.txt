# TODO to bring upto feature parity with the old duckduckhack VM
Switch to "dax" account
Get rid of "Ubuntu" account that exists in addition to the "Vagrant" account
configure sudo
Add Terminal icon on desktop.
Add Firefox icon on desktop
Configure Firefox for fixtracking.com

# TODO to make improvements over the old duckduckhack VM
command-line only VM
set to listen on NAT, instead of bridged
script on desktop for updating packages, including kernel
figure out kernel upgrades not breaking virtualbox guest additions and vmware tools
change vagrant SSH key at end
